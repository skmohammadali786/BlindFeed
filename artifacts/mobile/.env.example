EXPO_PUBLIC_API_URL=http://localhost:8080/api
# Optional fallback for production builds:
# EXPO_PUBLIC_DOMAIN=api.example.com
# For EAS Android/iOS preview/production builds, configure one of these
# with a public reachable API host (not localhost/private IP).
